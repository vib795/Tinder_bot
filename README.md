to run:
 - download chromedriver, unzip, move to `your working directory`
 - `pip install selenium`

create a secrets.py file with variables:
``` 
 username = 'your_username'
 password = 'your_password'
```